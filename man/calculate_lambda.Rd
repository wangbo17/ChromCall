% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_calculate_lambda.R
\name{calculate_lambda}
\alias{calculate_lambda}
\title{Calculate Genomic Lambda Value}
\usage{
calculate_lambda(
  x,
  counts_col = "counts",
  blacklist_col,
  rm_zero = TRUE,
  summary_fun = mean
)
}
\arguments{
\item{x}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} object containing count values and optional metadata.}

\item{counts_col}{Character. Column name containing count values. Default is \code{"counts"}.}

\item{blacklist_col}{Character (optional). Logical column indicating blacklisted regions. If NULL, defaults to \code{"blacklist"} if present.}

\item{rm_zero}{Logical. Whether to exclude zero-count regions before summarization. Default is TRUE.}

\item{summary_fun}{Function. Summary function to apply (e.g., \code{\link[=mean]{mean()}}, \code{\link[=median]{median()}}). Default is \code{\link[=mean]{mean()}}.}
}
\value{
A single numeric value representing the background lambda level.
}
\description{
Computes the genomic lambda value from a \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} object by summarizing a specified counts column.
Optionally excludes blacklisted regions and zero-count entries before summarization.
}
\examples{
# GRanges object with counts and blacklist
gr <- GenomicRanges::GRanges(
  seqnames = "chr1",
  ranges = IRanges::IRanges(start = c(1, 1001, 2001), width = 1000),
  counts = c(8, 0, 30),
  blacklist = c(FALSE, FALSE, TRUE)
)

# No blacklist filtering: 'blacklist_col' not specified
calculate_lambda(gr)

# With blacklist filtering: exclude third region
calculate_lambda(gr, blacklist_col = "blacklist")

# With blacklist and zero filtering: exclude third (blacklist) and second (zero)
calculate_lambda(gr, blacklist_col = "blacklist", rm_zero = TRUE)

# Use median instead of mean (no blacklist)
calculate_lambda(gr, summary_fun = median)

}
